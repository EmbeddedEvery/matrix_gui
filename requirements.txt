# WS2812 Matrix GUI - Python Dependencies
# Install with: pip install -r requirements.txt
# Or with conda: conda install -c conda-forge -r requirements.txt

# Core Streamlit framework
streamlit>=1.28.0

# Data processing and visualization
pandas>=2.0.0
numpy>=1.24.0

# BLE (Bluetooth Low Energy) support for device communication
bleak>=0.21.0

# Async support (usually included with Python 3.7+, but explicitly listed for clarity)
asyncio-contextmanager>=1.0.0

# Optional: for enhanced performance and dev features
watchdog>=2.3.0  # File monitoring for Streamlit hot reload
